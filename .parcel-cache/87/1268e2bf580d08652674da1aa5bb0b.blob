const cursor = document.querySelector('.cursor');

    document.addEventListener('mousemove', e =>{
      cursor.setAttribute("style", "top: "+e.pageY+"px");
      cursor.setAttribute("style", "left: "+e.pageX)

    })