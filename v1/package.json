{
  "name": "raconte",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html src/app/*.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build index.html src/app/*.html --no-content-hash --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "locomotive-scroll": "^4.1.1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0-beta.2",
    "cssnano": "^5.0.6",
    "postcss": "^8.3.5",
    "sass": "^1.34.1"
  },
  "description": ""
}
